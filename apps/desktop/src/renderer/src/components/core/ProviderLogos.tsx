/**
 * Service logomarks sourced from Simple Icons (via react-icons/si). They
 * render as SVGs with `fill="currentColor"`, so they pick up the surrounding
 * text token and adapt to light/dark mode automatically.
 *
 * Only the org's search lane (Brave) still carries a mark. Models come from
 * the org's one lane and carry no vendor mark.
 */

import { SiBrave } from 'react-icons/si'

export { SiBrave as BraveLogo }
